{
  "name": "zoom-app",
  "version": "1.0.0",
  "author": "Mihir Katariya",
  "type": "commonjs",
  "dependencies": {
    "express": "^4.18.2"
  },
  "scripts": {
    "start": "npm run build && node server.js",
    "build": "cd Front-end && npm install && npm run build && cd .."
  }
}
